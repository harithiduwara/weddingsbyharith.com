# Drop new photographs here

Save full-resolution originals into this folder, any filenames, then say so.
They get inspected, renamed, grouped into galleries and moved to
`photos/raw/`, which is where the build reads from.

This folder is ignored by git so nothing half-sorted reaches the repository.
